package animal_shelter.model.animals;

import java.time.LocalDate;

public class Cat extends Pet{
    public Cat(int id, String name, LocalDate birthDay, String color) {
        super(id, name, birthDay, color);

    }
}
